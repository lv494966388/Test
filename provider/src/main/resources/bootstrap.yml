





mybatis:
  mapper-locations: mapper/*.xml

spring:
  application:
    name: spring-cloud-provider

  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/lwb?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root


  cloud:
    config:
      label: master
      profile: dev
      discovery:
          enabled: true
          service-id: springcloud-config




server:
     port: 8082
